# Data (download from source / regenerate)
aggregated_reviews.csv

# Trained model artifacts (regenerated by running the notebook)
# NOTE: the Random Forest .pkl exceeds GitHub's 100 MB file limit
*.pkl

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Virtual environments
venv/
.venv/
env/

# OS / editor cruft
.DS_Store
